#changin prefix
set -g prefix C-x
unbind C-b

#index begin in 1 in windows
set -g base-index 1

#index begin in 1 in panes
setw -g pane-base-index 1

#reload conf
bind r source-file ~/.tmux.conf \; display "Conf Reloaded!"

#keep send prefix to app running inside tmux, pressinf prefix twice
bind C-x send-prefix

#enable mouse
set -g mouse on

#split windows
bind | split-window -h -c "#{pane_current_path}" #horizontal
bind - split-window -v -c "#{pane_current_path}" #vertical

#moving for panes
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

#cycle through panes
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

#resizing panes
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

#color
set -g default-terminal "screen-256color"

#-- statusbar --#
set -g status-interval 1
set -g renumber-windows on
set-window-option -g automatic-rename on
set-option -g set-titles on

setw -g mode-keys emacs
# this rule is good when I load predefined layout
#setw -g automatic-rename off

#-- colorscheme: Zenburn --#
# Palette aligned with zenburn.toml:
# background #3A3A3A, foreground #DCDCCC
# black      #1E2320, red        #D78787
# green      #60B48A, yellow     #DFAF8F
# blue       #506070, magenta    #DC8CC3
# cyan       #8CD0D3, bright blk #709080

# modes
setw -g clock-mode-colour "#DFAF8F"
set -g mode-style bg="#4F4F4F",fg="#DCDCCC"

# panes
set -g pane-border-style fg="#4F4F4F"
set -g pane-active-border-style fg="#709080"

# statusbar
set -g status-justify centre
set -g status-style bg="#3A3A3A",fg="#DCDCCC"
set -g status-left "#[bg=#4F4F4F,fg=#DCDCCC,bold] #S #[bg=#3A3A3A,fg=#709080,nobold] #[fg=#DFAF8F]#I:#P "
set -g status-left-length 30
set -g status-right "#[fg=#709080]load #[fg=#DFAF8F]#(cut -d ' ' -f 1-3 /proc/loadavg) #[fg=#709080]| #[fg=#C3BF9F]%Y-%m-%d %H:%M "
set -g status-right-length 80

setw -g window-status-format "#[fg=#709080] #I:#W#F "
setw -g window-status-style bg="#3A3A3A",fg="#709080"
setw -g window-status-current-format "#[bg=#4F4F4F,fg=#F0DFAF,bold] #I:#W#F "
setw -g window-status-current-style bg="#3A3A3A",fg="#DCDCCC"

# messages
set -g message-style bg="#4F4F4F",fg="#DCDCCC"
set -g message-command-style bg="#4F4F4F",fg="#F0DFAF"


#paste
bind C-y paste-buffer
bind C-c run "tmux save-buffer - | xclip -i -sel clipboard"
bind C-v run "tmux set-buffer \"$(xclip -o -sel clipboard)\"; tmux paste-buffer"

#maximizing panes
unbind Up
bind Up new-window -d -n tmp \; swap-pane -s tmp.0 \; select-window -t tmp
unbind Down
bind Down last-window \; swap-pane -s tmp.0 \; kill-window -t tmp


# tmux resurrect
#run-shell /home/luis/dev/tmux-resurrect-master/resurrect.tmux

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-copycat'

set -g @continuum-restore 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
